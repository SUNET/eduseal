# Need glibc-based image since protoc is built with glibc.
FROM golang:1.25-bookworm

WORKDIR /go/src/app

COPY Makefile .
RUN make install-tools \
	&& go clean -cache -modcache \
	&& rm -rf /root/.cache/go-build

